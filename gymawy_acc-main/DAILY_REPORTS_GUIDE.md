# 📊 دليل نظام التقارير اليومية

## نظرة عامة
نظام متكامل لإرسال تقارير يومية تلقائية عبر البريد الإلكتروني للمديرين مع لوحة تحكم كاملة للإدارة.

---

## ✨ المميزات الرئيسية

### 1. لوحة تحكم شاملة
- ✅ تفعيل/إيقاف التقارير بضغطة زر
- ⏰ تحديد موعد الإرسال بالدقيقة
- 👥 إدارة المستلمين (تفعيل/إيقاف لكل مستلم)
- 📋 اختيار أقسام التقرير المطلوبة
- 📧 إرسال تقرير تجريبي فوري

### 2. محتوى التقرير

#### 👥 الحضور والغياب
- إجمالي الموظفين
- عدد الحاضرين ✅
- عدد المتأخرين ⏰
- عدد الغائبين ❌
- عدد الموظفين في إجازة 🏝️

#### 💰 الملخص المالي
- إجمالي الإيرادات 📈
- إجمالي المصروفات 📉
- صافي الربح 💵

#### 📋 المهام
- المهام المكتملة اليوم ✅
- المهام قيد التنفيذ ⏳
- المهام المتأخرة ⚠️

#### 🔔 التنبيهات الذكية
- تنبيه عند ارتفاع نسبة الغياب (> 20%)
- تنبيه عند ارتفاع نسبة التأخير (> 15%)
- تنبيه عند تجاوز المصروفات للإيرادات
- تنبيه عند وجود مهام متأخرة

---

## 🚀 الإعداد والتشغيل

### 1. إعداد البريد الإلكتروني

#### لـ Gmail:
1. اذهب إلى [Google Account Settings](https://myaccount.google.com/)
2. Security → 2-Step Verification (فعّل المصادقة الثنائية)
3. App passwords → أنشئ كلمة مرور جديدة
4. انسخ كلمة المرور

#### تحديث ملف .env:
```env
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password-here
```

### 2. تشغيل النظام
```bash
cd backend
npm install
npm run dev
```

النظام سيبدأ تلقائياً في فحص الإعدادات كل دقيقة وإرسال التقارير في المواعيد المحددة.

---

## 📱 استخدام لوحة التحكم

### الوصول للوحة التحكم
1. سجل دخول كمدير عام أو مدير إداري
2. من القائمة الجانبية → **⚙️ إعدادات التقارير**

### الإعدادات المتاحة

#### 1. تفعيل/إيقاف التقارير
- زر كبير في أعلى الصفحة
- ✅ أخضر = مفعّل
- ⏸️ أحمر = متوقف

#### 2. تحديد موعد الإرسال
- اختر الوقت بالساعة والدقيقة
- مثال: 18:00 (6 مساءً)
- يتم الحفظ تلقائياً عند التغيير

#### 3. إدارة المستلمين
- قائمة بجميع المديرين
- نقطة خضراء = مفعّل
- نقطة رمادية = متوقف
- زر تفعيل/إيقاف لكل مستلم

#### 4. اختيار أقسام التقرير
- 👥 الحضور والغياب
- 💰 الملخص المالي
- 📋 المهام
- 🔔 التنبيهات
- انقر على أي قسم لتفعيله/إيقافه

#### 5. إرسال تقرير تجريبي
- زر **📧 إرسال تقرير تجريبي** في أعلى الصفحة
- يرسل تقرير فوري لبريدك الإلكتروني
- مفيد لاختبار التصميم والمحتوى

---

## 🔧 API Endpoints

### الحصول على الإعدادات
```http
GET /api/report-settings
Authorization: Bearer {token}
```

### تحديث الإعدادات
```http
PUT /api/report-settings
Authorization: Bearer {token}
Content-Type: application/json

{
  "enabled": true,
  "sendTime": "18:00",
  "reportSections": {
    "attendance": true,
    "financial": true,
    "tasks": true,
    "alerts": true
  }
}
```

### تفعيل/إيقاف التقارير
```http
PUT /api/report-settings/toggle
Authorization: Bearer {token}
Content-Type: application/json

{
  "enabled": true
}
```

### تحديث موعد الإرسال
```http
PUT /api/report-settings/send-time
Authorization: Bearer {token}
Content-Type: application/json

{
  "sendTime": "20:00"
}
```

### تحديث المستلمين
```http
PUT /api/report-settings/recipients
Authorization: Bearer {token}
Content-Type: application/json

{
  "recipients": [
    {
      "userId": "user-id",
      "email": "manager@company.com",
      "name": "Manager Name",
      "enabled": true
    }
  ]
}
```

### إرسال تقرير تجريبي
```http
POST /api/report-settings/test
Authorization: Bearer {token}
```

---

## 📁 هيكل الملفات

```
backend/src/
├── models/
│   └── ReportSettings.ts          # نموذج إعدادات التقارير
├── controllers/
│   └── reportSettings.controller.ts  # معالجات API
├── routes/
│   └── reportSettings.routes.ts   # مسارات API
├── services/
│   ├── dailyReport.service.ts     # جمع بيانات التقرير
│   └── email.service.ts           # إرسال البريد الإلكتروني
└── jobs/
    └── dailyReport.job.ts         # مهمة الجدولة

frontend/src/
└── pages/
    └── ReportSettings.tsx         # صفحة لوحة التحكم
```

---

## 🎨 تصميم البريد الإلكتروني

التقرير يأتي بتصميم HTML احترافي:
- 📱 Responsive Design
- 🎨 ألوان متناسقة مع هوية النظام
- 📊 بطاقات إحصائية واضحة
- 🔔 تنبيهات بارزة
- 📅 تاريخ ووقت واضح

---

## ⚙️ التخصيص

### تغيير تكرار الفحص
في `dailyReport.job.ts`:
```typescript
// الحالي: كل دقيقة
cron.schedule('* * * * *', async () => {

// كل 5 دقائق:
cron.schedule('*/5 * * * *', async () => {

// كل ساعة:
cron.schedule('0 * * * *', async () => {
```

### إضافة أقسام جديدة للتقرير
1. أضف البيانات في `dailyReport.service.ts`
2. أضف HTML في `email.service.ts`
3. أضف الخيار في `ReportSettings.tsx`

### تخصيص التنبيهات
في `dailyReport.service.ts`:
```typescript
// تغيير نسبة الغياب
if (absent > totalEmployees * 0.2) { // 20%

// تغيير نسبة التأخير
if (late > totalEmployees * 0.15) { // 15%
```

---

## 🐛 استكشاف الأخطاء

### التقرير لا يُرسل
1. ✅ تحقق من إعدادات البريد في `.env`
2. ✅ تأكد من تفعيل التقارير في لوحة التحكم
3. ✅ تحقق من موعد الإرسال
4. ✅ تأكد من تفعيل المستلمين
5. ✅ راجع logs في console

### البريد لا يصل
1. ✅ تحقق من صحة App Password
2. ✅ تحقق من البريد الإلكتروني للمستلم
3. ✅ راجع مجلد Spam
4. ✅ جرب إرسال تقرير تجريبي

### التصميم لا يظهر بشكل صحيح
1. ✅ بعض برامج البريد تحجب CSS
2. ✅ استخدم inline styles
3. ✅ اختبر على Gmail, Outlook, Yahoo

---

## 📝 ملاحظات مهمة

- ⚠️ التقرير يُرسل مرة واحدة فقط في اليوم
- ⚠️ يتم حفظ آخر موعد إرسال لتجنب التكرار
- ⚠️ المستلمون المتوقفون لا يتلقون التقارير
- ⚠️ الأقسام المتوقفة لا تظهر في التقرير
- ✅ يمكن إرسال تقارير تجريبية في أي وقت
- ✅ التغييرات تُطبق فوراً

---

## 🔐 الصلاحيات

- **المدير العام**: وصول كامل لجميع الإعدادات
- **المدير الإداري**: وصول كامل لجميع الإعدادات
- **باقي الأدوار**: لا يمكنهم الوصول

---

## 📞 الدعم

للمساعدة أو الاستفسارات:
- 📧 البريد الإلكتروني: support@gymmawy.net
- 📱 الهاتف: +20 123 456 7890

---

**تم التطوير بواسطة فريق جماوي 🚀**
